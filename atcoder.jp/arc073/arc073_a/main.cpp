#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include "/debug.h"
#else
#define db(...)
#endif

#define all(v) v.begin(), v.end()
#define pb push_back
using ll = long long;
constexpr char endl = '\n';
const int NAX = 2e5 + 5, MOD = 1000000007;


int64_t solveCase(int N, int64_t T, const vector<int64_t> & t) {
    // TODO: edit here
}

// generated by oj-template v4.7.2 (https://github.com/online-judge-tools/template-generator)
int main() 
{
#ifndef LOCAL
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
#endif
    int64_t N;
    int64_t T;
    cin >> N;
    vector<int64_t> t(N);
    cin >> T;
    for (int i = 0; i < N; ++i) {
        cin >> t[i];
    }
    auto ans = solveCase(N, T, t);
    cout << ans << endl;
    return 0;
}